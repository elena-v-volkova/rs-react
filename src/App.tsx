import './App.css';
import Form from './components/form/Form';
// import { create } from 'zustand';

function App() {
  // const useStore = create((set) => ({
  //   formRenderData: {},
  //   setNewData: (newData) => set({ newData }),
  // }));
  return <Form />;
}

export default App;
